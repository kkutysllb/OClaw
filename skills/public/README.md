# Public Skills

This directory holds read-only public skills shipped with the desktop bundle.

Public skills are shared across all users and cannot be modified at runtime
(see `local_skill_storage.py` with `KKOCLAW_PUBLIC_SKILLS_ONLY`).

Add new public skills as subdirectories here, each containing a `SKILL.md`.
